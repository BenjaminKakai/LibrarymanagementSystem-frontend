// src/BookCatalog/BookCatalog.js

import React from 'react';
import './BookCatalog.css'; // Importing the CSS file

const BookCatalog = () => {
  return (
    <div className="book-catalog-container">
      <h1>Our Book Collection</h1>
      {/* Future implementation: map through an array of books and display them */}
    </div>
  );
};

export default BookCatalog;
